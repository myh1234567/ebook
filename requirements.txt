edge-tts>=7.0
python-docx>=1.1.0
selenium>=4.20.0
openai>=1.20.0
Pillow>=10.0.0

# 声音克隆时才需要（会带 torch，约 3-4GB；不克隆就别装）
# f5-tts

# 上传 YouTube 时才需要（先不装也能生成视频）
# google-api-python-client
# google-auth-oauthlib
